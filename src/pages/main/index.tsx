import { Button } from '@/shared/ui';
import styles from './styles.module.scss';

export const MainPage = function MainPage() {
  return (
    <main className={styles.page}>
      qwe
      <Button variant="outlined">qwe</Button>
    </main>
  );
};
